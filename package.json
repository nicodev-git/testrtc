{
  "name": "twilio-group-thing",
  "version": "1.0.0",
  "description": "lets see how horrible their sdk is",
  "private": true,
  "main": "index.js",
  "scripts": {
    "deploy": "netlify deploy --prod --dir=build --site=0db468d8-46bf-4488-a322-6a8b2f661aae",
    "build": "browserify src/main.js -o build/main.js && browserify src/webex-login.js -o build/webex-login.js",
    "test": "eslint server.js src/",
    "start": "npm run build && node server",
    "dev": "env-cmd npm run start"
  },
  "author": "fippo",
  "license": "UNLICENSED",
  "dependencies": {
    "assert": "^2.0.0",
    "browserify": "^16.5.1",
    "eslint": "^7.3.1",
    "express": "^4.17.1",
    "redis": "^3.0.2",
    "twilio": "^3.46.0",
    "twilio-video": "^2.5.0",
    "uuid": "^8.2.0",
    "webex": "^1.114.0"
  },
  "devDependencies": {
    "env-cmd": "^10.1.0",
    "netlify-cli": "^2.54.0",
    "prettier": "^2.2.1"
  }
}
